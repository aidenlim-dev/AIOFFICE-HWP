name: Hello (debug)

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/hello.yml'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello from GitHub Actions"
