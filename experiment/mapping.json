{
  "template": "experiment/form_template.hwp",
  "_comment": "Authored by Claude. Field LABELS + profile KEY names + cell offsets only — NO personal values. The fill engine reads the actual values from the profile file at runtime.",
  "fields": [
    { "label": "①성",          "key": "name",    "col_offset": 1 },
    { "label": "주민등록번호",  "key": "rrn",     "col_offset": 1 },
    { "label": "③주",          "key": "address", "col_offset": 1 },
    { "label": "전화번호",      "key": "phone",   "col_offset": 1 }
  ]
}
